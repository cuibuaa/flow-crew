- id: choose_round
  role: campaign_planner
  scope:
    - docs/happymj_explore6/round_03_plan.json
  depends_on: []
  dependency_reasons: {}
  criterion_refs:
    - criterion_what_each_round_s_report_4_1efd2539
    - criterion_what_each_round_s_report_5_ed6f6c15
    - criterion_what_each_round_s_report_9_054b9401
  prompt_template: |
    Read tech_solution.md, iteration_log.md, the knowledge graph, current Explore 6 artifacts, prior campaign docs, ITERATION_LOG.md, and relevant happymj modules. Choose exactly one Round 3 mechanism distinct from Round 1 generic PG/MMD/PPO and Round 2 Deep-CFR neural regret, avoiding recorded dead ends and unchanged prior configurations. Write only docs/happymj_explore6/round_03_plan.json with frozen mechanism, gates, seed ranges, thresholds, sample sizing, feasibility quantities, support/no-candidate fallback, audit identity, and later evidence requirements. Do not open outcomes or write terminal artifacts. Put temporary one-off work only under the OS temp root.

- id: write_ship
  role: coder
  scope:
    - docs/happymj_explore6/ship_report.md
  depends_on:
    - choose_round
  dependency_reasons:
    choose_round: "Terminal materialization must wait until the current phase has either produced its plan or the framework has selected a terminal path."
  condition: research.terminalPath == docs/happymj_explore6/ship_report.md
  criterion_refs:
    - criterion_what_each_round_s_report_8_06f94983
    - criterion_what_each_round_s_report_10_f4d0b217
    - criterion_what_each_round_s_report_11_24e8ca77
  prompt_template: |
    Write only docs/happymj_explore6/ship_report.md if the framework selected this exact terminal path. Before writing, re-read reality_checks.md, run each declared check as a throughout invariant, run the confirmation command and make build/test/lint, and report direct exit codes/totals. Explain any check repair narrative in the report body, never in machine declarations. Do not write on continue.

- id: write_ceiling
  role: coder
  scope:
    - docs/happymj_explore6/ceiling_report.md
  depends_on:
    - choose_round
  dependency_reasons:
    choose_round: "Terminal materialization must wait until the current phase has either produced its plan or the framework has selected a terminal path."
  condition: research.terminalPath == docs/happymj_explore6/ceiling_report.md
  criterion_refs:
    - criterion_what_each_round_s_report_4_1efd2539
    - criterion_what_each_round_s_report_8_06f94983
    - criterion_what_each_round_s_report_10_f4d0b217
    - criterion_what_each_round_s_report_11_24e8ca77
  prompt_template: |
    Write only docs/happymj_explore6/ceiling_report.md if the framework selected this exact terminal path. Name the configured stopping condition that actually fired and the count satisfying it. Before writing, re-read reality_checks.md, run each declared check as a throughout invariant, and run make build/test/lint with direct exit codes/totals. Put check repair narrative in the report body. Do not write on continue.

- id: write_escalate
  role: coder
  scope:
    - docs/happymj_explore6/escalation_note.md
  depends_on:
    - choose_round
  dependency_reasons:
    choose_round: "Terminal materialization must wait until the current phase has either produced its plan or the framework has selected a terminal path."
  condition: research.terminalPath == docs/happymj_explore6/escalation_note.md
  criterion_refs:
    - criterion_what_each_round_s_report_8_06f94983
    - criterion_what_each_round_s_report_10_f4d0b217
    - criterion_what_each_round_s_report_11_24e8ca77
  prompt_template: |
    Write only docs/happymj_explore6/escalation_note.md if the framework selected this exact terminal path. Before writing, re-read reality_checks.md, run each declared check as a throughout invariant, and run make build/test/lint with direct exit codes/totals. State the blocker, failed alternatives, and why no truthful round continuation is possible. Do not write on continue.
