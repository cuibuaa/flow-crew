---
name: fc-status
description: Show the current FlowCrew run progress with the project CLI. Use when the user asks for FlowCrew status or invokes the fc-status workflow.
---
<!-- flowcrew-skill-revision: 2 -->

# fc-status — Check FlowCrew run progress

Show the current status of the latest FlowCrew run.

## Steps

Run this single command:
```bash
flowcrew status
```

That's it. Show the output to the user. Do NOT run additional commands or attempt to summarize — the output is already formatted.
